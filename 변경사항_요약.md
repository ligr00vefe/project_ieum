# 문의 게시판 구현 — 변경사항 요약

> 작업자: 민수  
> 브랜치: `minsu/dev`  
> 작업일: 2026-06-12

---

## 한 줄 요약

> 기존에 관리자용 문의 관리 페이지만 있던 상태에서,  
> **클라이언트(일반 사용자)가 직접 문의를 작성·조회·수정·삭제할 수 있는 게시판**을 새로 구현했습니다.  
> 비밀글, 카테고리 필터, 페이지네이션, 답변 상태 토글 기능이 포함됩니다.

---

## 변경 전 / 변경 후 비교

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 클라이언트 문의 페이지 | ❌ 없음 | ✅ `/inquiries` (목록/작성/상세/수정/삭제) |
| 비밀글 기능 | ❌ 없음 | ✅ 작성 시 비밀글 설정, 타인 마스킹 처리 |
| 답변 상태 토글 | ❌ 없음 | ✅ 관리자가 PENDING ↔ ANSWERED 전환 가능 |
| 관리자 카테고리 필터 | ❌ 없음 | ✅ 상태 필터 + 카테고리 필터 동시 적용 |
| 페이지네이션 | ❌ 없음 | ✅ 클라이언트·관리자 목록 모두 적용 |
| 헤더 "문의" 링크 | `/contact` (404) | `/inquiries` (정상) |

---

## 새로 추가된 파일 (6개)

### 백엔드

#### `src/main/java/com/project/ieum/dto/InquiryCreateForm.java` ← 신규
```
클라이언트가 문의 작성 시 사용하는 DTO
- title   : 제목 (필수, 최대 200자)
- body    : 본문 (필수)
- category: 카테고리 선택 (필수)
- secret  : 비밀글 여부 (기본값 false)
```

#### `src/main/java/com/project/ieum/controller/InquiryController.java` ← 신규
```
클라이언트 문의 게시판 전체 라우터
GET  /inquiries              → 목록 (카테고리 필터 + 페이지네이션)
GET  /inquiries/new          → 작성 폼
POST /inquiries/new          → 작성 처리
GET  /inquiries/{id}         → 상세 조회 (비밀글 접근 제어 포함)
POST /inquiries/{id}/reply   → 관리자가 클라이언트 화면에서 답변
GET  /inquiries/{id}/edit    → 수정 폼 (본인만)
POST /inquiries/{id}/edit    → 수정 처리
POST /inquiries/{id}/delete  → 삭제 (본인만)
```

### 프론트엔드 (Thymeleaf)

#### `src/main/resources/templates/inquiries/list.html` ← 신규
- 전체 문의 목록 테이블
- 카테고리 탭 필터 (전체 / 매칭 / 계정 / 서비스 / 기타)
- 타인의 비밀글 → 자물쇠 아이콘 + "비밀글입니다." 마스킹
- 본인 글 → "내 글" 뱃지 표시
- 답변 상태 뱃지 (대기: 주황색 / 완료: 초록색)
- 하단 페이지네이션 (현재 페이지 기준 앞뒤 2페이지, 최대 5개)

#### `src/main/resources/templates/inquiries/form.html` ← 신규
- 문의 작성 폼
- 카테고리 드롭다운, 제목 입력, 본문 입력
- 비밀글 체크박스

#### `src/main/resources/templates/inquiries/detail.html` ← 신규
- 문의 상세 보기
- 본인 글일 경우 수정 / 삭제 버튼 노출
- 관리자 접속 시 하단에 전용 영역 표시:
  - 답변 상태 관리 (PENDING ↔ ANSWERED 토글 버튼)
  - 답변 작성 / 수정 폼

#### `src/main/resources/templates/inquiries/edit.html` ← 신규
- 문의 수정 폼 (기존 제목·본문·카테고리·비밀글 값 자동 채움)

---

## 수정된 파일 (5개)

### 백엔드

#### `src/main/java/com/project/ieum/entity/inquiry/Inquiry.java` ← 수정
```
추가된 메서드:
- update(title, body, category, isSecret)  : 문의 내용 수정
- markPending()                             : 상태를 PENDING으로 되돌리기
  (기존에 markAnswered()만 있었음)
```

#### `src/main/java/com/project/ieum/repository/InquiryRepository.java` ← 수정
```
추가된 쿼리 메서드 (총 5개):
- findByCategoryOrderByCreatedAtDesc(category)              : 카테고리별 목록
- findAllByOrderByCreatedAtDesc(pageable)                   : 전체 페이지네이션
- findByCategoryOrderByCreatedAtDesc(category, pageable)    : 카테고리별 페이지네이션
- findByStatusOrderByCreatedAtDesc(status, pageable)        : 상태별 페이지네이션
- findByCategoryAndStatusOrderByCreatedAtDesc(cat, status, pageable) : 복합 필터
```

#### `src/main/java/com/project/ieum/service/admin/InquiryService.java` ← 수정
```
추가된 메서드 (총 8개):
- create(form, author)             : 클라이언트 문의 등록
- getByAuthor(author)              : 특정 유저 문의 목록 조회
- getByCategory(category)          : 카테고리별 목록 조회
- update(id, form, user)           : 본인 확인 후 수정 (타인이면 403)
- delete(id, user)                 : 본인 확인 후 삭제 (타인이면 403)
- toggleStatus(id)                 : PENDING ↔ ANSWERED 상태 토글
- getAllPaged(category, page)       : 클라이언트 목록 페이지네이션 (10개씩)
- getFilteredPaged(cat, status, page): 관리자 복합 필터 페이지네이션
```

#### `src/main/java/com/project/ieum/controller/admin/AdminInquiryController.java` ← 수정
```
변경 내용:
- list()에 category, page 파라미터 추가
- POST /{id}/status 엔드포인트 추가 (상태 토글)
```

### 프론트엔드

#### `src/main/resources/templates/admin/inquiries/list.html` ← 수정
```
추가된 내용:
- 카테고리 필터 탭 (상태 필터와 독립적으로 동작, 동시 적용 가능)
- 하단 페이지네이션 UI
```

#### `src/main/resources/templates/admin/inquiries/detail.html` ← 수정
```
변경된 내용:
- 상태 관리 영역을 별도 카드로 분리 (항상 노출)
- 카드 순서: 문의내용 → 상태관리 → 기존답변 → 답변폼
```

#### `src/main/resources/templates/layout/header.html` ← 수정
```
변경된 내용:
- 데스크탑 메뉴 "문의" 링크: /contact → /inquiries
- 모바일 메뉴 "문의" 링크: /contact → /inquiries
  (기존 /contact 경로에 컨트롤러가 없어 404 발생하던 문제 해결)
```

---

## 주요 기능 동작 흐름

```
[문의 작성]
사용자 → GET /inquiries/new → 폼 입력 → POST /inquiries/new
→ InquiryService.create() → DB 저장 (status=PENDING) → 상세 페이지로 이동

[관리자 답변]
관리자 → /admin/inquiries/{id} → 답변 입력 → POST /admin/inquiries/{id}/reply
→ InquiryService.reply() → InquiryReply 저장 + status=ANSWERED

[상태 토글]
관리자 → POST /admin/inquiries/{id}/status
→ InquiryService.toggleStatus() → PENDING ↔ ANSWERED 전환

[비밀글 보호]
타인이 비밀글 상세 접근 시도 → 컨트롤러에서 차단 → 목록으로 리다이렉트
타인의 글 수정/삭제 시도    → 서비스에서 ForbiddenException 발생
```

---

## 기존 기능 변경 없음 확인

- `Inquiry`, `InquiryReply`, `InquiryCategory`, `InquiryStatus` 엔티티 구조 그대로 유지
- 기존 `AdminInquiryController`의 `reply()` 로직 변경 없음
- 기존 `InquiryService`의 `getAll()`, `getById()`, `reply()` 메서드 변경 없음
- 신규 메서드 추가만 수행, 기존 메서드 덮어쓰기 없음
