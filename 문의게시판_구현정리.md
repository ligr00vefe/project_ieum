# 문의 게시판 구현 정리

> 작성일: 2026-06-12
> 브랜치: minsu/dev

---

## 전체 구조 한눈에 보기

```
클라이언트(USER/CAREGIVER)          관리자(ADMIN)
─────────────────────────          ──────────────────────
GET  /inquiries          목록       GET  /admin/inquiries        목록
GET  /inquiries/new      작성폼     GET  /admin/inquiries/{id}   상세
POST /inquiries/new      작성처리   POST /admin/inquiries/{id}/reply   답변등록
GET  /inquiries/{id}     상세       POST /admin/inquiries/{id}/status  상태토글
GET  /inquiries/{id}/edit  수정폼
POST /inquiries/{id}/edit  수정처리
POST /inquiries/{id}/delete 삭제
POST /inquiries/{id}/reply  답변(관리자만)
```

---

## 새로 만든 파일

### 1. `dto/InquiryCreateForm.java`
**경로:** `src/main/java/com/project/ieum/dto/InquiryCreateForm.java`

```java
@Getter @Setter
public class InquiryCreateForm {

    @NotBlank(message = "제목을 입력해주세요.")
    @Size(max = 200, message = "제목은 200자 이내로 입력해주세요.")
    private String title;

    @NotBlank(message = "내용을 입력해주세요.")
    private String body;

    @NotNull(message = "카테고리를 선택해주세요.")
    private InquiryCategory category;

    private boolean secret = false;
}
```

**왜 필요한가?**
- 기존 `InquiryForm`(관리자 답변용)은 `body` 하나만 있음
- 클라이언트가 문의를 작성할 때는 제목, 본문, 카테고리, 비밀글 여부가 모두 필요
- 역할이 다른 폼은 DTO를 분리하는 것이 원칙

**유효성 검증 어노테이션 설명**
- `@NotBlank` : null, 빈 문자열(""), 공백(" ") 모두 거부
- `@Size(max=200)` : 글자 수 제한
- `@NotNull` : null만 거부 (선택하지 않은 경우 방지)

---

### 2. `controller/InquiryController.java`
**경로:** `src/main/java/com/project/ieum/controller/InquiryController.java`

**역할:** 클라이언트가 문의를 작성·조회·수정·삭제하는 모든 요청을 처리

```java
@Controller
@RequestMapping("/inquiries")   // 모든 경로는 /inquiries 로 시작
@RequiredArgsConstructor        // final 필드 자동 생성자 주입 (Lombok)
public class InquiryController {

    private final InquiryService inquiryService;
    private final CurrentUserService currentUserService; // 현재 로그인 유저 가져오기
```

**각 메서드 설명**

| 메서드 | 경로 | 하는 일 |
|--------|------|---------|
| `list()` | GET /inquiries | 전체 목록 + 카테고리 필터 + 페이지네이션 |
| `newForm()` | GET /inquiries/new | 작성 폼 표시 |
| `submit()` | POST /inquiries/new | 작성 처리 후 상세로 이동 |
| `detail()` | GET /inquiries/{id} | 상세 조회 + 비밀글 접근 제어 |
| `reply()` | POST /inquiries/{id}/reply | 관리자만 답변 등록 가능 |
| `editForm()` | GET /inquiries/{id}/edit | 수정 폼 (본인만) |
| `editSubmit()` | POST /inquiries/{id}/edit | 수정 처리 |
| `delete()` | POST /inquiries/{id}/delete | 삭제 처리 |

**비밀글 접근 제어 로직**
```java
boolean isOwner = inquiry.getAuthor().getId().equals(currentUser.getId());
boolean isAdmin = currentUser.getRole() == UserRole.ADMIN;

if (inquiry.getIsSecret() && !isOwner && !isAdmin) {
    ra.addFlashAttribute("error", "비밀글은 작성자 본인만 조회할 수 있습니다.");
    return "redirect:/inquiries";  // 목록으로 튕겨냄
}
```

**페이지네이션 처리**
```java
Page<Inquiry> inquiryPage = inquiryService.getAllPaged(cat, page);
int startPage = Math.max(0, page - 2);  // 현재 페이지 기준 앞 2개
int endPage   = Math.min(totalPages - 1, page + 2);  // 현재 페이지 기준 뒤 2개
// → 최대 5개 페이지 번호 표시
```

---

### 3. 템플릿 파일 4종
**경로:** `src/main/resources/templates/inquiries/`

#### `list.html` — 목록 페이지
- 카테고리 탭 필터 (전체/매칭/계정/서비스/기타)
- 비밀글 마스킹 처리
```html
<!-- 타인의 비밀글: 마스킹 -->
<tr th:if="${inquiry.isSecret and inquiry.author.id != currentUserId and !isAdmin}">
    <td>비밀글입니다.</td>
</tr>

<!-- 정상 표시 -->
<tr th:unless="${inquiry.isSecret and inquiry.author.id != currentUserId and !isAdmin}">
    <td th:text="${inquiry.title}">제목</td>
</tr>
```
- 페이지네이션 UI (이전/번호/다음)

#### `form.html` — 작성 폼
- 공지사항 카드 레이아웃 구조 참고
- 카테고리 `<select>` 드롭다운
- 비밀글 체크박스

#### `detail.html` — 상세 페이지
- 본인 글이면 수정/삭제 버튼 노출
- 관리자 접속 시 하단에 전용 영역 렌더링
```html
<div th:if="${isAdmin}">
    <!-- 상태 토글 버튼 -->
    <!-- 답변 등록/수정 폼 -->
</div>
```

#### `edit.html` — 수정 폼
- form.html과 동일 구조, 기존 값이 바인딩된 상태로 표시
- `th:field="*{title}"` 으로 기존 값 자동 채움

---

## 수정한 파일

### 4. `entity/inquiry/Inquiry.java`
**경로:** `src/main/java/com/project/ieum/entity/inquiry/Inquiry.java`

**추가된 메서드 2개**
```java
// 문의 내용 수정 (필드가 private이라 직접 접근 불가 → 메서드로 수정)
public void update(String title, String body, InquiryCategory category, Boolean isSecret) {
    this.title    = title;
    this.body     = body;
    this.category = category;
    this.isSecret = isSecret;
}

// 상태를 미답변으로 되돌리기 (기존에 markAnswered()만 있었음)
public void markPending() { this.status = InquiryStatus.PENDING; }
```

**왜 엔티티 안에 메서드를 두는가?**
- JPA 엔티티 필드는 `private`이라 외부에서 직접 수정 불가
- 비즈니스 로직을 엔티티 안에 두는 것이 도메인 주도 설계(DDD) 패턴
- 수정 로직이 여러 곳에 흩어지지 않음

---

### 5. `service/admin/InquiryService.java`
**경로:** `src/main/java/com/project/ieum/service/admin/InquiryService.java`

**추가된 메서드 정리**

```java
// ① 클라이언트 문의 등록
public Inquiry create(InquiryCreateForm form, User author) {
    Inquiry inquiry = Inquiry.builder()
            .author(author)
            .category(form.getCategory())
            .title(form.getTitle())
            .body(form.getBody())
            .isSecret(form.isSecret())
            .build();
    return inquiryRepository.save(inquiry);
}

// ② 본인 문의 수정 (타인이면 ForbiddenException)
public void update(Long id, InquiryCreateForm form, User user) {
    Inquiry inquiry = getById(id);
    if (!inquiry.getAuthor().getId().equals(user.getId())) {
        throw new ForbiddenException("본인의 문의만 수정할 수 있습니다.");
    }
    inquiry.update(form.getTitle(), form.getBody(), form.getCategory(), form.isSecret());
}

// ③ 본인 문의 삭제 (타인이면 ForbiddenException)
public void delete(Long id, User user) {
    Inquiry inquiry = getById(id);
    if (!inquiry.getAuthor().getId().equals(user.getId())) {
        throw new ForbiddenException("본인의 문의만 삭제할 수 있습니다.");
    }
    inquiryRepository.delete(inquiry);
}

// ④ 상태 토글 PENDING ↔ ANSWERED
public void toggleStatus(Long id) {
    Inquiry inquiry = getById(id);
    if (inquiry.getStatus() == InquiryStatus.PENDING) {
        inquiry.markAnswered();
    } else {
        inquiry.markPending();
    }
}

// ⑤ 작성자별 목록 조회
public List<Inquiry> getByAuthor(User author) { ... }

// ⑥ 카테고리별 목록 조회
public List<Inquiry> getByCategory(InquiryCategory category) { ... }

// ⑦ 클라이언트 목록 페이지네이션
public Page<Inquiry> getAllPaged(InquiryCategory category, int page) { ... }

// ⑧ 관리자 목록 복합 필터 + 페이지네이션
public Page<Inquiry> getFilteredPaged(InquiryCategory category, InquiryStatus status, int page) { ... }
```

**서비스 레이어에서 권한 체크하는 이유**
- 컨트롤러가 아닌 서비스에서 체크하면 다른 곳에서 같은 메서드를 호출해도 항상 권한이 보장됨
- 컨트롤러는 요청만 받고 서비스에 위임하는 단순한 역할 유지

---

### 6. `repository/InquiryRepository.java`
**경로:** `src/main/java/com/project/ieum/repository/InquiryRepository.java`

**추가된 쿼리 메서드**
```java
// 카테고리 필터 (List 반환)
List<Inquiry> findByCategoryOrderByCreatedAtDesc(InquiryCategory category);

// 페이지네이션 전용 (Page 반환)
Page<Inquiry> findAllByOrderByCreatedAtDesc(Pageable pageable);
Page<Inquiry> findByCategoryOrderByCreatedAtDesc(InquiryCategory category, Pageable pageable);
Page<Inquiry> findByStatusOrderByCreatedAtDesc(InquiryStatus status, Pageable pageable);
Page<Inquiry> findByCategoryAndStatusOrderByCreatedAtDesc(InquiryCategory category, InquiryStatus status, Pageable pageable);
```

**Spring Data JPA 메서드 이름 규칙**
```
findBy + 필드명 + 조건 + OrderBy + 정렬필드 + 방향

findByCategoryAndStatus  → WHERE category = ? AND status = ?
OrderByCreatedAtDesc     → ORDER BY created_at DESC
```
- 메서드 이름만 정확히 쓰면 SQL을 직접 작성하지 않아도 JPA가 자동으로 쿼리 생성
- `Pageable` 파라미터를 추가하면 자동으로 LIMIT/OFFSET 처리

---

### 7. `controller/admin/AdminInquiryController.java`
**추가된 내용**
```java
// 상태 토글 엔드포인트
@PostMapping("/{id}/status")
public String toggleStatus(@PathVariable Long id, RedirectAttributes ra) {
    inquiryService.toggleStatus(id);
    ra.addFlashAttribute("message", "답변 상태가 변경되었습니다.");
    return "redirect:/admin/inquiries/" + id;
}

// list() 에 카테고리 필터 + 페이지네이션 파라미터 추가
@GetMapping
public String list(@RequestParam(required = false) String status,
                   @RequestParam(required = false) String category,
                   @RequestParam(defaultValue = "0") int page,
                   Model model) { ... }
```

---

### 8. 관리자 템플릿 2종

**`admin/inquiries/list.html`**
- 카테고리 필터 탭 추가 (상태 필터와 독립적으로 동작, 동시 적용 가능)
- 페이지네이션 추가

**`admin/inquiries/detail.html`**
- 상태 관리 영역을 별도 카드로 분리 (항상 노출)
```
[문의 내용 카드]
[답변 상태 관리 카드] ← 항상 보임, 토글 버튼 포함
[등록된 답변 카드]   ← 답변 있을 때만 표시
[답변 작성/수정 폼]  ← 항상 보임
```

---

### 9. `layout/header.html`
**변경 내용**
```html
<!-- 변경 전 -->
<a th:href="@{/contact}">문의</a>

<!-- 변경 후 -->
<a th:href="@{/inquiries}">문의</a>
```
- 데스크탑 메뉴, 모바일 메뉴 둘 다 변경
- `/contact` 경로에 컨트롤러가 없어서 404 발생했던 문제 해결

---

## 보안 처리 정리

| 상황 | 처리 방식 |
|------|----------|
| 비로그인 `/inquiries` 접근 | SecurityConfig `anyRequest().authenticated()` 가 자동으로 로그인 페이지로 이동 |
| 타인 비밀글 상세 접근 | 컨트롤러에서 확인 후 목록으로 리다이렉트 |
| 타인 글 수정/삭제 시도 | 서비스에서 `ForbiddenException` 발생 |
| 관리자가 아닌데 답변 시도 | 컨트롤러에서 role 체크 후 무시 |

---

## 데이터 흐름 요약

```
[사용자 문의 작성]
  브라우저 → POST /inquiries/new
  → InquiryController.submit()
  → InquiryService.create()
  → InquiryRepository.save()
  → DB 저장 (status = PENDING)

[관리자 답변 등록]
  브라우저 → POST /admin/inquiries/{id}/reply
  → AdminInquiryController.reply()
  → InquiryService.reply()
  → InquiryReply 저장 + inquiry.markAnswered()
  → DB 저장 (status = ANSWERED)

[사용자 답변 확인]
  브라우저 → GET /inquiries/{id}
  → InquiryController.detail()
  → inquiry.reply != null → 파란 박스로 답변 표시
```

---

## 페이지네이션 동작 원리

```java
// 10개씩, 최신순 정렬
PageRequest.of(page, 10, Sort.by("createdAt").descending())

// Page 객체가 가진 정보
inquiryPage.getContent()      // 현재 페이지 데이터 목록
inquiryPage.getTotalPages()   // 전체 페이지 수
inquiryPage.isFirst()         // 첫 페이지 여부
inquiryPage.isLast()          // 마지막 페이지 여부
```

템플릿에서 페이지 번호 표시 (현재 페이지 기준 앞뒤 2개, 최대 5개):
```html
<th:block th:each="i : ${#numbers.sequence(startPage, endPage)}">
    <a th:href="@{/inquiries(page=${i})}" th:text="${i + 1}">1</a>
</th:block>
```
